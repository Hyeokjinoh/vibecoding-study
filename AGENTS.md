# AGENTS.md

이 파일은 **도구 중립 표준**입니다. Codex, Cursor, Copilot, Gemini CLI 등이
네이티브로 읽습니다. Claude Code 는 이 파일을 자동으로 읽지 않으므로,
루트 `CLAUDE.md` 상단에서 `@AGENTS.md` 로 불러옵니다.
(이 이중 구조 자체가 3장 Guides 의 실물 예제입니다.)

## 이 저장소는 무엇인가

바이브 코딩 학습 자료입니다. 저장소 자체가 하네스 엔지니어링의 예제이므로
`.claude/` 설정을 지우거나 단순화하지 마세요.

## 절대 규칙

1. **외부 자료의 문장을 옮기지 않습니다.** 개념을 이해하고 한국어로 새로 씁니다.
   인용은 15단어 미만 정의구에 한하며 출처 URL 을 붙입니다.
2. `docs/_facts/` 에 근거가 없는 기술적 주장은 쓰지 않습니다.
3. 수집·집필·검증은 서로 다른 에이전트가 맡습니다. 집필한 주체가 검증하지 않습니다.
4. `seed/todo-cli/` 의 버그는 실습 재료입니다. 시키지 않으면 고치지 않습니다.

## 검증

```bash
./scripts/check-docs.sh                    # 문서 구조·링크 검증
python -m pytest seed/todo-cli/tests -q    # 시드 테스트 (실패가 정상)
```
