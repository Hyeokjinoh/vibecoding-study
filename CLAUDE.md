# 프로젝트 컨텍스트

@AGENTS.md

> 위 한 줄이 도구 중립 표준인 `AGENTS.md` 를 이 컨텍스트로 불러온다.
> Claude Code 는 `AGENTS.md` 를 자동으로 읽지 않으므로 이렇게 다리를 놓아야 한다.
> 아래는 Claude Code 에만 해당하는 추가 규칙이다.

이 저장소는 **바이브 코딩 학습 자료**다. 동시에 이 저장소 자체가
자료가 가르치는 하네스 엔지니어링의 **실물 예제**다.
`.claude/` 아래 설정은 교보재이므로, 지우거나 단순화하지 않는다.

## 저장소 구조

| 경로 | 역할 |
|---|---|
| `docs/` | 학습 문서 본문. GitHub Pages 로 퍼블리싱된다 |
| `docs/_facts/` | 수집 에이전트가 남긴 근거 노트. **집필의 유일한 근거** |
| `.claude/agents/` | 수집·집필·검증 에이전트 정의 (Part 2 교보재) |
| `seed/todo-cli/` | 실습용 시드 코드. **의도적으로 결함이 있다** |
| `.github/workflows/` | 문서 검증 CI (Sensor 예제) |

## 절대 규칙

### 1. 저작권 안전 (최우선)
- 외부 자료의 **문장을 옮기지 않는다.** 개념을 이해하고 한국어로 새로 쓴다.
- 인용은 15단어 미만 정의구에 한하며, 인용부호와 출처 URL을 반드시 붙인다.
- 외부 이미지를 가져오지 않는다. 도식은 Mermaid 로 직접 그린다.
- 새 참고 자료를 쓰면 `SOURCES.md` 에 라이선스와 함께 추가한다.

### 2. 근거 없는 서술 금지
`docs/_facts/` 에 근거가 없는 기술적 주장은 쓰지 않는다.
근거가 없으면 `<!-- NEED-FACT: 질문 -->` 을 남기고 넘어간다.

### 3. 역할 분리 (maker / checker)
- 자료 수집은 `collector` 에이전트가 한다.
- 집필은 `writer` 에이전트가 한다. **writer 에게는 웹 도구를 주지 않는다.**
- 검증은 `verifier` 에이전트가 한다. **집필한 주체가 검증하지 않는다.**

### 4. 시드 코드
`seed/todo-cli/` 의 버그는 **실습 재료다. 시키지 않으면 고치지 않는다.**

## 문서 문체

- 독자: 컴퓨터공학 전공 신입 개발자
- 영어 용어는 첫 등장 시 `하네스(harness)` 로 병기, 이후 한국어 통일
- 한 문단 4줄 이내
- 사실과 견해를 섞지 않는다. 견해는 `> 💭 **필자 견해**` 블록으로 분리

## 자주 쓰는 명령

```bash
python -m pytest seed/todo-cli/tests -q   # 시드 코드 테스트 (현재 실패한다)
./scripts/check-docs.sh                   # 문서 구조·링크 로컬 검증
```
